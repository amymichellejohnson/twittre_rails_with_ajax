<%= render "layouts/errors", object: @tweet %>

<%= form_for @tweet do |f| %>
  <p>
    <%= f.label "Author a new tweet" %>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit id: "submit" %>
  </p>
<% end %>
